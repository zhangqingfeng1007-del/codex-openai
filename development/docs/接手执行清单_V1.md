# 接手执行清单 V1

## 零、拉代码
```bash
git clone git@github.com:zhangqingfeng1007-del/codex-openai.git
cd codex-openai
git pull origin main
```

## 一、打开 Claude Code / Codex
仓库根目录有 `CLAUDE.md`，打开工作区后会自动加载。

## 二、先读这 3 个文件
1. `development/docs/项目移交说明_V1.md`
2. `development/docs/extraction_pipeline_status_v4.md`
3. `development/CLAUDE.md` 如存在，以仓库根目录 `CLAUDE.md` 为准

## 三、第一天环境验证命令
### 1. Phase 1 入口
```bash
cd /Users/zqf-openclaw/codex-openai/development
python3 scripts/build_all.py --input-dir /Users/zqf-openclaw/Desktop/开发材料/10款重疾 --output-manifest data/manifests/manifest_latest.json
```

### 2. Tier A 提取
```bash
python3 scripts/extract_tier_a_rules.py data/manifests/manifest_latest.json /tmp/tier_a_rule_candidates_check.json --blocks-dir data/blocks
```

### 3. Tier B 提取
```bash
python3 scripts/extract_responsibility_fields.py
```

### 4. dry-run 匹配
```bash
python3 scripts/write_to_db.py --dry-run --candidates data/extractions/tier_b_responsibility_candidates_v1.json
```

## 四、当前接手入口
直接从：
- `development/scripts/extract_responsibility_fields.py`
继续开发。

## 五、当前本地备份
本地历史上下文不在 Git 跟踪中，需要额外交接：
- `development/memory/latest`
- `development/memory/202603290939`

## 六、当前主线判断
1. Tier A 已打通
2. Tier B 当前 31/31 命中标准值库
3. OCR 暂挂，不是当前重点
4. 保障说明类难度最高，后做
