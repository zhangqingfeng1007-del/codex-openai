# Claude Code 修订任务书

目标文件：
`/Users/zqf-openclaw/code/aix-engine/docs/AIX智能体需求与开发文档-v4.1.md`

你的任务不是重写一份全新文档，而是在保留 v4.1 当前优点的前提下，系统修订这份文档，使其同时满足：

1. 体现“最终目标是全能力保险智能体”，覆盖重疾险、医疗险、寿险、增额终身寿、分红险等完整险种能力。
2. 明确“一期开发只是为了先跑通重疾险主链路”，不能把一期范围写成最终范围。
3. 把二期及以后的目标提前规划进文档结构中，但不要把后续能力误写成一期已支持能力。
4. 把一期的后续开发步骤写清楚，结合当前已完成内容，明确接下来还要开发什么。
5. 消除文档内部冲突、歧义和会导致开发返工的表述。

请直接修改原文件，不要新建第二份平行版本。

## 一、修订总原则

### 1. 产品目标和开发范围必须分层表达

文档必须明确区分这三层概念，不能混写：

- 最终目标：AIX 要建设的是“全能力保险智能体”，不是只做重疾险推荐工具。
- 一期范围：当前版本只实现重疾险主链路跑通，用于验证架构、交互、推荐链路、精算链路和数据链路。
- 后续路线：二期、三期及以后逐步扩展到其他险种和更完整能力。

请在前文核心位置直接写清楚：

- 本项目的最终形态是全能力保险智能体。
- 一期不是目标收缩，而是实施顺序上的第一步。
- 一期仅支持重疾险，不代表系统架构只服务重疾险。

### 2. 一期文档要可开发，长期文档要可扩展

修改后的文档必须同时满足：

- 对当前开发同学来说，能知道这周要做什么、做到什么算完成。
- 对后续扩展来说，能看出系统为什么这样设计，以及以后如何扩展到其他险种。

不要只写宏大目标，不要只写一期局部修补。
要做到“目标完整、范围克制、路径清晰”。

## 二、必须修改的内容

### 1. 重写项目目标与分阶段策略

请修改“项目背景与目标”“开发目标与分阶段策略”相关章节，明确写成下面的逻辑：

- 产品最终目标：全能力保险智能体。
- 一期目标：先完成重疾险场景下的可用主链路。
- 一期价值：验证核心对话架构、统一规划 schema、产品检索、评分、精算、卡片展示、前端入口、移动端体验。
- 二期及以后：按险种和能力模块逐步扩展，不需要重构一期核心架构。

这里要避免当前文档给人的误解：

- 误解 A：系统就是重疾险系统。
- 误解 B：预算分配和规划能力已经支持所有险种推荐。
- 误解 C：二期只是“以后再说”，缺少提前设计。

### 2. 一期范围和长期能力的口径必须统一

当前文档最大问题之一，是一期边界和长期能力写法冲突。
你必须把它改成下面的口径：

- 一期产品推荐和精算定价，只基于重疾险产品库。
- 但智能体的规划视角可以面向家庭保障全局，只是在一期中，对非重疾险仅做方向性规划建议，不做基于产品库的精准推荐和保费测算。
- 如果你认为“方向性规划建议”这句话仍会引起误解，也可以进一步收紧为：
  一期仅对非重疾险输出“后续保障优先级提示”，不输出具体险种推荐结果卡。

无论你采用哪种写法，都必须在文档中明确：

- 一期支持什么。
- 一期不支持什么。
- 对于医疗险、寿险等非一期险种，系统可以说到什么程度，不能说到什么程度。

这部分必须同步影响：

- 预算分配章节
- 推荐结果章节
- 风险披露章节
- 一期险种边界章节
- 验收清单

### 3. 修复预算分配与险种边界冲突

当前 `budget_allocation.by_coverage` 包含：

- `critical_illness`
- `medical`
- `life`
- `other`

但一期又只支持重疾险，这会导致实现和验收冲突。

请你处理成一个一致方案，推荐做法如下：

- 保留 `BudgetAllocation` 的扩展性结构，因为系统最终要支持全险种。
- 但文档必须明确：
  - 一期前端展示和推荐结果中，重疾险相关字段为主；
  - 非重疾险字段即使保留在接口层，也属于后续扩展预留；
  - 一期不能因为接口预留而被视为“已支持医疗险/寿险推荐”。

如果你认为更好的方案是拆成“当前已用字段”和“后续预留字段”，也可以，但必须写得足够清楚，让开发和验收不会打架。

### 4. 补一章“一期实施顺序 / 后续开发步骤”

这是本次必须新增或强化的重点。

请结合当前 v4.1 已写内容，增加一个清晰章节，名字可以是：

- “一期实施顺序”
- “当前完成情况与下一步开发”
- “一期剩余开发任务”

要求写清楚：

- 现在已经基本确定或已经完成的内容有哪些。
- 还缺哪些关键开发项才能形成一期闭环。
- 哪些属于当前必须完成。
- 哪些属于一期后半段或二期准备项。

建议至少覆盖这些内容：

- 首页与对话入口收口
- 路由卡逻辑收口
- 对话规划与填写信息规划共用 schema
- 预算测算与预算展示
- NeedsReportCard / RecommendationCard / Compare / Conclusion / Risk 卡片收口
- 一期重疾险检索、评分、精算主链路
- 非重疾险扩展预留点
- 验收与联调顺序

请不要只写“后续优化”四个字，要写成开发可以照着排期和执行的内容。

### 5. 修复产品主数据表名冲突

当前文档里存在产品主数据命名不一致问题，例如：

- 架构图里写的是 `cmb_product`
- 后文又写成 `aix_engine.products`

你必须统一成真实表名。

要求：

- 全文只保留一个主口径。
- 如果历史命名仍需保留，请注明“历史命名 / 旧表名 / 映射关系”，不要并列写成当前事实。
- 架构图、数据库主数据章节、检索说明、验收口径保持一致。

如果你无法从代码中确认真实表名，请在文档中显式标注“待确认”，但不要继续写成两个都像真的。

### 6. 修复推荐结果卡片数量和命名冲突

当前“卡片化结构（5张）”和实际列出的卡片数量不一致。

请你统一：

- 卡片总数
- 卡片名称
- 验收清单里的对应叫法

要求全文只保留一套说法，不要出现：

- 标题说 5 张，明细其实是 6 或 7 张
- `RecommendationConclusionCard` / `ConclusionCard` / `RiskCard` 混用

### 7. 修复章节编号和结构细节问题

请顺手修复文档结构问题，例如：

- `2.3` 后又出现 `2.2`
- 容易误导的章节名
- 验收清单中重复条目

这些问题虽然不是架构问题，但会降低文档可信度，必须收口。

### 8. 处理“无需查阅其他文档可直接开发”这句话

当前这句话风险很高，因为文档里对某些内容仍然只有概念，没有实现规格，例如：

- JWT 鉴权
- AES 加密
- 会话管理
- 客户档案

请你二选一：

- 要么把这些内容补足到足以开发；
- 要么把文档表述改成更准确的说法，比如：
  “本文档覆盖当前版本核心开发要求，个别基础设施细节按现有工程实现为准。”

不要让文档承诺超出它实际覆盖范围。

## 三、推荐的文档表达方式

为了让文档既能指导一期开发，又能承载长期规划，建议采用以下写法：

### 1. 在前文直接增加“最终目标 vs 当前范围”说明

可以用一小节明确写：

- 最终目标：全能力保险智能体
- 当前范围：一期仅跑通重疾险
- 架构要求：一期设计必须支持后续险种扩展

### 2. 为一期和后续阶段分别写清楚

至少明确三层：

- 一期：重疾险跑通
- 二期：扩展若干险种或能力
- 长期目标：全险种、全能力、B 端开放、性能提升

### 3. 用“当前支持 / 规划支持 / 暂不支持”表达边界

凡是容易冲突的地方，优先使用这三类标签来表达：

- 当前支持
- 规划支持
- 暂不支持

这样可以避免把“预留接口”误写成“已经完成能力”。

## 四、修改完成后的自检要求

请在修改文档后，自己先做一轮自检，至少确认以下问题全部为“是”：

1. 文档是否明确写出了“最终目标是全能力保险智能体”？
2. 文档是否明确写出了“一期仅支持重疾险跑通”？
3. 文档是否明确写出了“二期及以后扩展方向”？
4. 一期边界和预算分配口径是否不再冲突？
5. 产品主数据表名是否全文统一？
6. 推荐结果卡片数量和命名是否全文统一？
7. 验收清单是否没有重复和自相矛盾的条目？
8. 一期后续开发步骤是否足够具体，开发可据此排期？
9. 文档是否没有把“预留能力”写成“已实现能力”？
10. 文档是否仍然保留了 v4.1 原本已经明确的好内容，而不是退回到空泛方案文档？

## 五、输出要求

请直接修改：
`/Users/zqf-openclaw/code/aix-engine/docs/AIX智能体需求与开发文档-v4.1.md`

修改完成后，请输出：

1. 你改了哪些章节
2. 你如何处理“一期范围”和“长期目标”的关系
3. 你如何处理预算分配与险种边界冲突
4. 你如何统一产品主数据表名
5. 你如何统一推荐卡片数量与验收口径
6. 还剩哪些必须由人确认的事项

注意：

- 不要新建平行版本文档
- 不要只改一两处字句
- 要做结构性修订
- 不要把文档改成空泛汇报材料
- 最终结果要让后续开发 AI 和真人开发都能直接使用
